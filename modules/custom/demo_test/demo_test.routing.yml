# Menu items corresponding to these URLs are defined separately in the
# fapi_example.links.menu.yml file.
demo_test.dtform:
  path: 'demo-test/dtform'
  defaults:
    _form:  '\Drupal\demo_test\Form\DtForm'
    _title: 'demo test form'
  requirements:
    _permission: 'access demo test'

